<h1><%=@user.fname %></h1>

<%= form_for @post do |f| %>
  <%= f.label :message %>
  <br>
  <%= f.text_area :body %>
  <br>
  <!-- <%= f.hidden_field :user_id, :value => current_user.id %> -->

  <%=f.submit :Post %>
<% end %>
